#include "main.h"
#include <stdio.h>
/**
 *main - Entry point
 *@argc: Argument character
 *@argv: Argument vector
 *Return: Always 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
int i;

for (i = 0; i < argc; i++)
{
printf("%c\n", argv[i]);
}
return (0);
}
