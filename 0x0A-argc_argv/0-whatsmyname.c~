#include "main.h
#include <stdlib.h">
/**
 *main - Entry point
 *@argc: Argument int
 *@argv: Argument array
 *Return: Always 0
 */
int main(int argc, char *argv)
{
int i = 0;
while (i <= argc)
{
printf("%c", argv[i]);
return (0);
}
}
