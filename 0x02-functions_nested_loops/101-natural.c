#include "main.h"
/**
 * main - Entry point
 *
 * Return: 0(success)
 */
void natural.c(int)
{
	int n,i;

	for (n = 0; n < 1024; n++)
	{
		for (n % 3 == 0; || n % 5 == 0)
		{
			i += n;
		}
	}
	_putchar('\n');
}
