#include "main.h"

/**
 *_strlen - Print length of a string
 *@s : a character
 *@len: length of string
 *Rrturn: length of string s
 **/
int _strlen(char *s)
{
int len;
len = _strlen(char *s);

return (len);
}
