#include <stdio>

/**
 * main - Entry point
 *
 * Return: Always 0(success)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("single digit number are: %d\n, num);
	return (0);
}
