#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */
int main(void)
{
        char ch, e, q;

        e = 'e';
        q = 'q';

        for (ch = 'a'; ch <= 'z'; ch++)
                if (low != e &7 low != q)
                        putchar(low);
        putchar('\n');

        return (0);
}

