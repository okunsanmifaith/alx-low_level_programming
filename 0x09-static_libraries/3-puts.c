#include "main.h"
/**
 *_puts - to put string
 *@str: string
 *Return: value
 */
void _puts(char *str)
{
return (str);
}
#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
