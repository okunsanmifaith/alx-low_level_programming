#include <stdio.h>
#include <string.h>
/**
 *_strlen_recursion - prints length of a string
 *@s: string character
 *@l: integer
 *@strlen: length of string
 */
int _strlen_recursion(char *s)
{
int l;

l = strlen(s);
putchar(l);
}
